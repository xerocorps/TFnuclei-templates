detail:
  links:
  - https://github.com/vulhub/vulhub/tree/master/spark/unacc
expression: r0()
manual: true
name: poc-yaml-spark-webui-unauth
rules:
  r0:
    expression: response.status == 200 && response.body.bcontains(b"<title>Spark")
      && response.body.bcontains(b"<strong>URL:</strong> spark:")
    request:
      cache: true
      method: GET
      path: /
transport: http
