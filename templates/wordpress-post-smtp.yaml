http:
- extractors:
  - group: 1
    internal: true
    name: internal_detected_version
    part: body
    regex:
    - (?i)Stable.tag:\s?([\w.]+)
    type: regex
  - group: 1
    name: detected_version
    part: body
    regex:
    - (?i)Stable.tag:\s?([\w.]+)
    type: regex
  matchers:
  - dsl:
    - compare_versions(internal_detected_version, concat("< ", last_version))
    name: outdated_version
    type: dsl
  - part: body
    regex:
    - (?i)Stable.tag:\s?([\w.]+)
    type: regex
  matchers-condition: or
  method: GET
  path:
  - '{{BaseURL}}/wp-content/plugins/post-smtp/readme.txt'
  payloads:
    last_version: helpers/wordpress/plugins/post-smtp.txt
id: wordpress-post-smtp
info:
  author: ricardomaia
  metadata:
    plugin_namespace: post-smtp
    wpscan: https://wpscan.com/plugin/post-smtp
  name: "Post SMTP \u2013 WP SMTP Plugin with Email Logs and Mobile App for Failure\
    \ Notifications \u2013 Gmail SMTP, Office 365, Brevo, Mailgun, Amazon SES and\
    \ more Detection"
  reference:
  - https://wordpress.org/plugins/post-smtp/
  severity: info
  tags: tech,wordpress,wp-plugin,top-200,discovery
