id: drupal-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all drupal related nuclei templates on
    a given target.
  name: Wordpress Security Checks
workflows:
- matchers:
  - name: drupal
    subtemplates:
    - tags: drupal
  template: technologies/tech-detect.yaml
