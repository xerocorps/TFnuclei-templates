id: apereo-cas-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Apereo CAS related nuclei templates
    on a given target.
  name: Apereo CAS Security Checks
workflows:
- subtemplates:
  - tags: cas
  template: exposed-panels/cas-login.yaml
