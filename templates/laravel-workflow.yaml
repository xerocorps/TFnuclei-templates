id: laravel-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Laravel related nuclei templates on
    a given target.
  name: Laravel Security Checks
workflows:
- matchers:
  - name: laravel
    subtemplates:
    - tags: laravel
  template: technologies/tech-detect.yaml
