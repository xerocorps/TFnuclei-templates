id: apache-workflow
info:
  author: philippedelteil
  description: A simple workflow that runs all Apache related nuclei templates on
    a given target.
  name: Apache workflow
workflows:
- subtemplates:
  - tags: apache
  template: technologies/apache/apache-detect.yaml
