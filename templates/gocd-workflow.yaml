id: gocd-workflow
info:
  author: dhiyaneshDk
  description: A simple workflow that runs all GoCD Pipeline related nuclei templates
    on a given target.
  name: GoCD Security Checks
workflows:
- subtemplates:
  - tags: gocd
  template: exposed-panels/gocd-login.yaml
