id: gitlist-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all GitList related nuclei templates on
    a given target.
  name: GitList Security Checks
workflows:
- matchers:
  - name: gitlist
    subtemplates:
    - tags: gitlist
  template: technologies/tech-detect.yaml
