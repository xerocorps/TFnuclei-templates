id: centos-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Centos WebPanel related nuclei templates
    on a given target.
  name: Centos WebPanel Security Checks
workflows:
- subtemplates:
  - tags: centos
  template: exposed-panels/cwp-webpanel.yaml
