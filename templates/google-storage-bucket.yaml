file:
- extensions:
  - all
  extractors:
  - regex:
    - '[a-z0-9.-]+\.appspot\.com'
    type: regex
id: google-storage-bucket
info:
  author: Thabisocn
  name: Detect Mobile apps using Google storage Bucket
  severity: info
