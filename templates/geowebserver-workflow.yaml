id: geowebserver-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all GeoWebServer related nuclei templates
    on a given target.
  name: GeoWebServer Security Checks
workflows:
- subtemplates:
  - tags: geowebserver
  template: http/technologies/geo-webserver-detect.yaml
