http:
- extractors:
  - group: 1
    internal: true
    name: version
    part: body
    regex:
    - '(?mi)Stable tag: ([0-9.]+)'
    type: regex
  - group: 1
    name: version
    part: body
    regex:
    - '(?mi)Stable tag: ([0-9.]+)'
    type: regex
  matchers:
  - status:
    - 200
    type: status
  - part: body
    type: word
    words:
    - get-url-cron
  - dsl:
    - compare_versions(version, '<= 1.4.7')
    type: dsl
  matchers-condition: and
  max-redirects: 3
  method: GET
  path:
  - '{{BaseURL}}/wp-content/plugins/get-url-cron/readme.txt'
  redirects: true
id: get-url-cron-430f85d960f2884d8bfd863968961c8f
info:
  author: topscoder
  classification:
    cve-id: null
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:N/I:L/A:N
    cvss-score: 4.3
  description: The Get URL Cron plugin for WordPress is vulnerable to Cross-Site Request
    Forgery in versions up to, and including, 1.4.7. This is due to missing or incorrect
    nonce validation on the geturlcron_action_handle function called via an init hook.
    This makes it possible for unauthenticated attackers to modify many of the plugin's
    settings and create cron jobs, via a forged request granted they can trick a site
    administrator into performing an action such as clicking on a link.
  metadata:
    fofa-query: wp-content/plugins/get-url-cron/
    google-query: inurl:"/wp-content/plugins/get-url-cron/"
    shodan-query: 'vuln:'
  name: Get URL Cron <= 1.4.7 - Cross-Site Request Forgery via geturlcron_action_handle
  reference:
  - https://plugins.trac.wordpress.org/browser/get-url-cron/tags/1.4.8/geturlcron.php#L780
  - https://plugins.trac.wordpress.org/browser/get-url-cron/tags/1.4.7/geturlcron.php#L778
  severity: medium
  tags: cve,wordpress,wp-plugin,get-url-cron,medium
