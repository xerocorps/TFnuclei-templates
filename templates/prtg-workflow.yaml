id: prtg-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all PRTG related nuclei templates on a
    given target.
  name: PRTG Security Checks
workflows:
- subtemplates:
  - tags: prtg
  template: technologies/prtg-detect.yaml
