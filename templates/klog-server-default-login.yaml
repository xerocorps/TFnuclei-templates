http:
- attack: pitchfork
  host-redirects: true
  matchers:
  - condition: and
    dsl:
    - contains_all(body, "<title>KLog Server", "ADMIN", "Dashboard")
    - status_code == 200
    type: dsl
  max-redirects: 3
  payloads:
    password:
    - admin
    username:
    - admin
  raw:
  - 'POST /actions/entree.php HTTP/1.1

    Host: {{Hostname}}

    Content-Type: application/x-www-form-urlencoded


    user={{username}}&pswd={{password}}&action=login

    '
  - 'GET /index.php HTTP/1.1

    Host: {{Hostname}}

    '
id: klog-server-default-login
info:
  author: s4e-io
  description: 'KLog Server default login credentials were discovered.

    '
  metadata:
    max-request: 2
    product: klog_server
    runzero-match: any(each(service["html.titles"]), {# matches "KLog Server"})
    vendor: klogserver
  name: KLog Server - Default Login
  reference:
  - https://www.klogserver.com/
  severity: high
  tags: default-login,klog-server,vuln
