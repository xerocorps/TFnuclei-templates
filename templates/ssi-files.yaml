file:
- extensions:
  - all
  extractors:
  - regex:
    - (?i)([a-z0-9-_]+).(shtml|shtm|stm)
    type: regex
id: ssi-files
info:
  author: ayadim
  name: detect SSI files can lead to SSII
  severity: info
  tags: file,bbh,bbh-ssii,bbh-check1
