id: yapi-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all YApi related nuclei templates on a
    given target.
  name: YApi Security Checks
workflows:
- matchers:
  - name: yapi
    subtemplates:
    - tags: yapi
  template: technologies/fingerprinthub-web-fingerprints.yaml
