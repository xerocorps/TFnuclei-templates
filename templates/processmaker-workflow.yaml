id: processmaker-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all ProcessMaker related nuclei templates
    on a given target.
  name: ProcessMaker Security Checks
workflows:
- matchers:
  - name: processmaker
    subtemplates:
    - tags: processmaker
  template: technologies/fingerprinthub-web-fingerprints.yaml
