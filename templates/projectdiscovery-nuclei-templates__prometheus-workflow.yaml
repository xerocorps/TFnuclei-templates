id: prometheus-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Prometheus related nuclei templates
    on a given target.
  name: Prometheus Security Checks
workflows:
- subtemplates:
  - tags: prometheus
  template: http/exposed-panels/prometheus-exposed-panel.yaml
