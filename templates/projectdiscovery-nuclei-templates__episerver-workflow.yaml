id: episerver-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all EpiServer related nuclei templates
    on a given target.
  name: EpiServer Security Checks
workflows:
- matchers:
  - name: episerver
    subtemplates:
    - tags: episerver
  template: http/technologies/fingerprinthub-web-fingerprints.yaml
