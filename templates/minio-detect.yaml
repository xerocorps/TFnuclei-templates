id: minio-detect
info:
  author: pikpikcu
  name: Minio panel detect
  severity: info
  tags: panel
requests:
- matchers:
  - condition: or
    part: body
    type: word
    words:
    - <title>MinIO Browser</title>
    - <title>Minio Browser</title>
  - status:
    - 200
    type: status
  matchers-condition: and
  method: GET
  path:
  - '{{BaseURL}}/minio/login'
