id: mantisbt-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all MantisBT related nuclei templates on
    a given target.
  name: MantisBT Security Checks
workflows:
- subtemplates:
  - tags: mantisbt
  template: http/exposed-panels/mantisbt-panel.yaml
