id: aviatrix-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Aviatrix Controller related nuclei
    templates on a given target.
  name: Aviatrix Controller Security Checks
workflows:
- subtemplates:
  - tags: aviatrix
  template: exposed-panels/aviatrix-panel.yaml
