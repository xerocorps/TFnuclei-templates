file:
- extensions:
  - all
  extractors:
  - regex:
    - (?i)stripe(.{0,20})?[sr]k_live_[0-9a-zA-Z]{24}
    type: regex
id: stripe-api-key
info:
  author: gaurang
  name: Stripe API Key
  severity: high
  tags: file,keys,token,stripe
