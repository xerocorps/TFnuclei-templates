file:
- extensions:
  - all
  extractors:
  - regex:
    - xox[baprs]-([0-9a-zA-Z]{10,48})?
    type: regex
id: slack-api
info:
  author: gaurang
  name: Slack API Key
  severity: high
  tags: file,keys,token,slack
