id: adminer-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Adminer related nuclei templates on
    a given target.
  name: Adminer Security Checks
workflows:
- subtemplates:
  - tags: adminer
  template: exposed-panels/adminer-panel.yaml
