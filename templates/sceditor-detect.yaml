http:
- matchers:
  - condition: and
    type: word
    words:
    - <title>Ticket BBCode editor - SCEditor</title>
    - title="SCEditor"
  - status:
    - 200
    type: status
  matchers-condition: and
  method: GET
  path:
  - '{{BaseURL}}'
id: sceditor-detect
info:
  author: pikpikcu
  metadata:
    max-request: 1
  name: SCEditor Detect
  severity: info
  tags: tech,sceditor
