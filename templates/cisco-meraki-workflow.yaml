id: cisco-meraki-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Cisco Meraki related nuclei templates
    on a given target.
  name: Cisco Meraki Security Checks
workflows:
- matchers:
  - name: meraki
    subtemplates:
    - tags: meraki
  template: technologies/favicon-detection.yaml
