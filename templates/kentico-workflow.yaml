id: kentico-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Kentico CMS related nuclei templates
    on a given target.
  name: Kentico CMS Security Checks
workflows:
- matchers:
  - name: kentico
    subtemplates:
    - tags: kentico
  template: technologies/fingerprinthub-web-fingerprints.yaml
