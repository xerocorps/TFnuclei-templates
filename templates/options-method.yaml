id: options-method
info:
  author: pdteam
  name: Allowed Options Method
  severity: info
  tags: misc,generic
requests:
- extractors:
  - group: 1
    part: header
    regex:
    - 'Allow: ([A-Z, ]+)'
    type: regex
  method: OPTIONS
  path:
  - '{{BaseURL}}'
