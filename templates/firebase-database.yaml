file:
- extensions:
  - all
  extractors:
  - regex:
    - '[a-z0-9.-]+\.firebaseio\.com'
    - '[a-z0-9.-]+\.firebaseapp\.com'
    type: regex
id: firebase-database
info:
  author: gaurang
  name: Firebase Database Detect
  severity: info
