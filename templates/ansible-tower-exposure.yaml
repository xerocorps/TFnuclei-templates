id: ansible-tower-exposure
info:
  author: pdteam
  name: Ansible Tower Exposure
  severity: low
  tags: panel
requests:
- matchers:
  - condition: and
    type: word
    words:
    - <title>Ansible Tower</title>
    - ansible-main-menu
  method: GET
  path:
  - '{{BaseURL}}'
