id: sourcebans-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all SourceBans related nuclei templates
    on a given target.
  name: SourceBans Security Checks
workflows:
- matchers:
  - name: sourcebans
    subtemplates:
    - tags: sourcebans
  template: technologies/fingerprinthub-web-fingerprints.yaml
