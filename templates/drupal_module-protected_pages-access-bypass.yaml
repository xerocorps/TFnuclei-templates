http:
- extractors:
  - group: 1
    name: version
    part: body
    regex:
    - version = "([0-9]+\.x-[0-9]+\.[0-9]+)"
    type: regex
  - dsl:
    - compare_versions(version, '<= 1.06')
    type: dsl
  matchers:
  - condition: and
    part: body
    regex:
    - version = "([0-9]+\.x-[0-9]+\.[0-9]+)"
    type: regex
  - condition: and
    status:
    - 200
    type: status
  - condition: and
    part: body
    type: word
    words:
    - protected_pages
  matchers-condition: and
  max-redirects: 3
  method: GET
  path:
  - '{{BaseURL}}/sites/all/modules/protected_pages/protected_pages.info'
  redirects: true
id: drupal_module-protected_pages-access-bypass
info:
  author: me
  description: This module enables you to secure any page with a password. The module
    does not sufficiently restrict access to the page content.
  metadata:
    created_at: '2025-01-09'
    fofa-query: /sites/all/modules/protected_pages/
    google-query: inurl:'/sites/all/modules/protected_pages/
    impact: medium
    security-risk: "Critical 16\u2009\u2215\u200925 AC:Basic/A:None/CI:Some/II:Some/E:Theoretical/TD:All"
    type: indicator
    vulnerability: access-bypass
  name: drupal_module-protected_pages-access-bypass
  reference:
  - https://www.drupal.org/sa-contrib-2023-013
  severity: medium
  tags: drupal
