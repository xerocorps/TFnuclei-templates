id: saltstack-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all SaltStack related nuclei templates
    on a given target.
  name: SaltStack Security Checks
workflows:
- matchers:
  - name: saltstack
    subtemplates:
    - tags: saltstack
  template: http/technologies/fingerprinthub-web-fingerprints.yaml
