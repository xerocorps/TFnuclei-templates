file:
- extensions:
  - all
  extractors:
  - regex:
    - (?i)twilio(.{0,20})?SK[0-9a-f]{32}
    type: regex
id: twilio-api
info:
  author: gaurang
  name: Twilio API Key
  severity: high
  tags: file,keys,token
