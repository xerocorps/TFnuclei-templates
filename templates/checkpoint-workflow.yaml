id: checkpoint-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Checkpoint related nuclei templates
    on a given target.
  name: Checkpoint Security Checks
workflows:
- subtemplates:
  - tags: checkpoint
  template: exposed-panels/checkpoint-panel.yaml
