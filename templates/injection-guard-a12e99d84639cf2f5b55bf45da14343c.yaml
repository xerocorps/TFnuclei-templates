http:
- extractors:
  - group: 1
    internal: true
    name: version
    part: body
    regex:
    - '(?mi)Stable tag: ([0-9.]+)'
    type: regex
  - group: 1
    name: version
    part: body
    regex:
    - '(?mi)Stable tag: ([0-9.]+)'
    type: regex
  matchers:
  - status:
    - 200
    type: status
  - part: body
    type: word
    words:
    - injection-guard
  - dsl:
    - compare_versions(version, '<= 1.2.1')
    type: dsl
  matchers-condition: and
  max-redirects: 3
  method: GET
  path:
  - '{{BaseURL}}/wp-content/plugins/injection-guard/readme.txt'
  redirects: true
id: injection-guard-a12e99d84639cf2f5b55bf45da14343c
info:
  author: topscoder
  classification:
    cve-id: null
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:L/A:N
    cvss-score: 5.3
  description: The Injection Guard plugin for WordPress is vulnerable to unauthorized
    modification of data due to a missing capability check on the ig_update function
    in versions up to, and including, 1.2.1. This makes it possible for authenticated
    attackers, with subscriber-level permissions and above, to update the plugin's
    whitelist.
  metadata:
    fofa-query: wp-content/plugins/injection-guard/
    google-query: inurl:"/wp-content/plugins/injection-guard/"
    shodan-query: 'vuln:'
  name: Injection Guard <= 1.2.1 - Missing Authorization to Whitelist Update
  reference:
  - https://plugins.trac.wordpress.org/changeset/2910840/injection-guard
  severity: medium
  tags: cve,wordpress,wp-plugin,injection-guard,medium
