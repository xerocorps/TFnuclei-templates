id: graphite-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Graphite related nuclei templates on
    a given target.
  name: Graphite Security Checks
workflows:
- subtemplates:
  - tags: graphite
  template: http/exposed-panels/graphite-browser.yaml
