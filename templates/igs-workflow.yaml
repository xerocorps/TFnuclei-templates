id: igs-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all SAP Internet Graphics Server (IGS)
    related nuclei templates on a given target.
  name: SAP Internet Graphics Server (IGS) Security Checks
workflows:
- subtemplates:
  - tags: igs
  template: technologies/sap/sap-igs-detect.yaml
