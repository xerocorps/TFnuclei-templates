http:
- extractors:
  - group: 1
    name: version_by_generator
    regex:
    - (?m)https:\/\/wordpress.org\/\?v=([0-9.]+)
    type: regex
  - group: 1
    name: version_by_js
    regex:
    - wp-emoji-release\.min\.js\?ver=((\d+\.?)+)\b
    type: regex
  - group: 1
    name: version_by_css
    regex:
    - install\.min\.css\?ver=((\d+\.?)+)\b
    type: regex
  matchers:
  - dsl:
    - compare_versions(version_by_generator, '>= 3.8.1', '<= 3.8.1')
    - compare_versions(version_by_js, '>= 3.8.1', '<= 3.8.1')
    - compare_versions(version_by_css, '>= 3.8.1', '<= 3.8.1')
    type: dsl
  - status:
    - 200
    type: status
  matchers-condition: and
  max-redirects: 2
  method: GET
  path:
  - '{{BaseURL}}'
  - '{{BaseURL}}/wp-admin/install.php'
  - '{{BaseURL}}/feed/'
  - '{{BaseURL}}/?feed=rss2'
  redirects: true
  stop-at-first-match: true
id: Yes-b01cddb47ead59017f6465d44536eb45
info:
  author: topscoder
  classification:
    cve-id: null
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:H/UI:N/S:U/C:H/I:H/A:H
    cvss-score: 7.2
  description: WordPress Core, in versions less than 3.8.2, is vulnerable to SQL Injection
    via the 'links_recently_updated_time' parameter. This can only be exploited by
    administrative users and above.
  metadata:
    shodan-query: 'vuln:'
  name: WordPress Core < 3.8.2 - SQL Injection
  reference:
  - https://advisories.dxw.com/advisories/sqli-in-wordpress-3-6-1/
  - https://wordpress.org/news/2014/04/wordpress-3-8-2/
  severity: high
  tags: cve,wordpress,wp-core,high
