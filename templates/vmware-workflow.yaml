id: vmware-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all VMware Pipeline related nuclei templates
    on a given target.
  name: VMware Security Checks
workflows:
- subtemplates:
  - tags: vmware
  template: technologies/vmware/vmware-detect.yaml
