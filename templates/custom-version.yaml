id: custom-version
info:
  author: pdteam
  name: Basic SSL Request
  severity: info
ssl:
- address: '{{Host}}:{{Port}}'
  matchers:
  - part: response
    type: word
    words:
    - tls12
  max_version: tls12
  min_version: tls12
