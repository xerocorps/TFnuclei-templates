id: nginx-status
info:
  author: dhiyaneshDK
  name: Nginx Status Page
  severity: low
  tags: misconfig,nginx
requests:
- matchers:
  - type: word
    words:
    - 'Active connections:'
  - status:
    - 200
    type: status
  matchers-condition: and
  method: GET
  path:
  - '{{BaseURL}}/nginx_status'
