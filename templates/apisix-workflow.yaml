id: apisix-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Apache Apisix related nuclei templates
    on a given target.
  name: Apache Apisix Security Checks
workflows:
- subtemplates:
  - tags: apisix
  template: exposed-panels/apache/apache-apisix-panel.yaml
