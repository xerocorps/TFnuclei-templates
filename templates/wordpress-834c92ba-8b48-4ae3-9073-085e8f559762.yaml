http:
- extractors:
  - group: 1
    name: version_by_generator
    regex:
    - (?m)https:\/\/wordpress.org\/\?v=([0-9.]+)
    type: regex
  - group: 1
    name: version_by_js
    regex:
    - wp-emoji-release\.min\.js\?ver=((\d+\.?)+)\b
    type: regex
  - group: 1
    name: version_by_css
    regex:
    - install\.min\.css\?ver=((\d+\.?)+)\b
    type: regex
  matchers:
  - dsl:
    - compare_versions(version_by_generator, '>= 6.2', '< 6.2.1')
    - compare_versions(version_by_js, '>= 6.2', '< 6.2.1')
    - compare_versions(version_by_css, '>= 6.2', '< 6.2.1')
    type: dsl
  - status:
    - 200
    type: status
  matchers-condition: and
  max-redirects: 2
  method: GET
  path:
  - '{{BaseURL}}'
  - '{{BaseURL}}/wp-admin/install.php'
  - '{{BaseURL}}/feed/'
  - '{{BaseURL}}/?feed=rss2'
  redirects: true
  stop-at-first-match: true
id: wordpress-834c92ba-8b48-4ae3-9073-085e8f559762
info:
  author: topscoder
  classification:
    cve-id: null
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:L/I:L/A:N
    cvss-score: 6.4
  description: 'WordPress Core failed to sufficiently sanitize block attributes in
    versions up to, and including, 6.2. This makes it possible for authenticated attackers
    with contributor-level and above permissions to embed arbitrary content in HTML
    comments on the page, though Cross-Site Scripting may be possible when combined
    with an additional vulnerability.

    '
  metadata:
    shodan-query: 'vuln:'
  name: 'WordPress Core < 6.2.1 - Insufficient Sanitization of Block Attributes

    '
  reference:
  - https://github.com/topscoder/nuclei-wordfence-cve
  - https://www.wordfence.com/threat-intel/vulnerabilities/id/834c92ba-8b48-4ae3-9073-085e8f559762?source=api-prod
  severity: low
  tags: cve,wordpress,wp-core,low,production
