id: metinfo-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all MetInfo related nuclei templates on
    a given target.
  name: MetInfo Security Checks
workflows:
- matchers:
  - name: metinfo
    subtemplates:
    - null
  template: technologies/fingerprinthub-web-fingerprints.yaml
