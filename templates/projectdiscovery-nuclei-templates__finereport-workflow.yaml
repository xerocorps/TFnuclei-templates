id: finereport-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all FineReport related nuclei templates
    on a given target.
  name: FineReport Security Checks
workflows:
- matchers:
  - name: finereport
    subtemplates:
    - tags: finereport
  template: http/technologies/fingerprinthub-web-fingerprints.yaml
