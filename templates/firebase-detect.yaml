id: firebase-detect
info:
  author: organiccrap
  name: firebase detect
  severity: low
requests:
- matchers:
  - part: body
    type: word
    words:
    - Could not parse auth token
  method: GET
  path:
  - '{{BaseURL}}/.settings/rules.json?auth=FIREBASE_SECRET'
