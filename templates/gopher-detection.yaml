id: gopher-detection
info:
  author: pry0cc
  name: Gopher Detection
  severity: info
  tags: network,gopher
network:
- host:
  - '{{Hostname}}'
  - '{{Host}}:70'
  inputs:
  - data: "\r\n"
  matchers:
  - dsl:
    - contains(to_lower(raw), 'gopher')
    type: dsl
