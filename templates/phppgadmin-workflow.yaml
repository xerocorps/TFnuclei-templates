id: phppgadmin-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all phpPgAdmin related nuclei templates
    on a given target.
  name: phpPgAdmin Security Checks
workflows:
- matchers:
  - name: phppgadmin
    subtemplates:
    - tags: phppgadmin
  template: technologies/tech-detect.yaml
