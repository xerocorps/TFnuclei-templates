id: iplanet-imap-detect
info:
  author: pussycat0x
  description: 'iPlanet Messaging Server is a powerful, standards-based Internet messaging
    server designed for high-capacity, reliable handling of the messaging needs.

    '
  metadata:
    fofa-query: app="iPlanet-Messaging-Server-5.2" && protocol="imap"
  name: iplanet messaging imap protocol
  severity: info
  tags: network,imap
network:
- extractors:
  - part: body
    regex:
    - iPlanet Messaging Server ([0-9.]+)
    type: regex
  host:
  - '{{Hostname}}'
  - '{{Host}}:110'
  inputs:
  - data: '

      '
  matchers:
  - type: word
    words:
    - iPlanet Messaging Server
