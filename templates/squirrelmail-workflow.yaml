id: squirrelmail-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all SquirrelMail related nuclei templates
    on a given target.
  name: SquirrelMail Security Checks
workflows:
- subtemplates:
  - tags: squirrelmail
  template: exposed-panels/squirrelmail-login.yaml
