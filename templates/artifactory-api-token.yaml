id: artifactory-api-token
info:
  author: Ice3man
  name: Artifactory API Token Disclosure
  severity: info
  tags: exposure,token,artifactory
requests:
- extractors:
  - part: body
    regex:
    - (?:\s|=|:|"|^)AKC[a-zA-Z0-9]{10,}
    type: regex
  method: GET
  path:
  - '{{BaseURL}}'
