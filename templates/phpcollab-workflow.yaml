id: phpcollab-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all PhpCollab related nuclei templates
    on a given target.
  name: PhpCollab Security Checks
workflows:
- subtemplates:
  - tags: phpcollab
  template: http/exposed-panels/phpcollab-panel.yaml
