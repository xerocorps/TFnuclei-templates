id: newrelic-rest-api-key
info:
  author: Ice3man
  name: REST API Key Disclosure
  severity: info
  tags: exposure,token
requests:
- extractors:
  - part: body
    regex:
    - (?i)NRRA-[a-f0-9]{42}
    type: regex
  method: GET
  path:
  - '{{BaseURL}}'
