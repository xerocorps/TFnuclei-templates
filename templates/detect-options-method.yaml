id: detect-options-method
info:
  author: pdteam
  name: Detect enabled OPTIONS methods
  severity: info
  tags: misc
requests:
- extractors:
  - group: 1
    part: header
    regex:
    - 'Allow: ([A-Z, ]+)'
    type: regex
  method: OPTIONS
  path:
  - '{{BaseURL}}'
