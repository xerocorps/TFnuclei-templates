id: rconfig-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all rConfig related nuclei templates on
    a given target.
  name: rConfig Security Checks
workflows:
- matchers:
  - name: rconfig
    subtemplates:
    - tags: rconfig
  template: technologies/fingerprinthub-web-fingerprints.yaml
