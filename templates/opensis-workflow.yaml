id: opensis-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all OpenSIS related nuclei templates on
    a given target.
  name: OpenSIS Security Checks
workflows:
- subtemplates:
  - tags: opensis
  template: exposed-panels/opensis-panel.yaml
