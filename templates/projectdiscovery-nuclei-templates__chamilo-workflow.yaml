id: chamilo-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Chamilo LMS related nuclei templates
    on a given target.
  name: Chamilo LMS Security Checks
workflows:
- matchers:
  - name: chamilo
    subtemplates:
    - tags: chamilo
  template: http/technologies/tech-detect.yaml
