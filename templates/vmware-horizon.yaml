id: vmware-horizon
info:
  author: pdteam
  name: VMware Horizon
  severity: info
  tags: vmware,horizon
requests:
- matchers:
  - status:
    - 200
    type: status
  - type: word
    words:
    - VMware Horizon
  matchers-condition: and
  max-redirects: 2
  method: GET
  path:
  - '{{BaseURL}}'
  redirects: true
