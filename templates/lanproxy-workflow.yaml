id: lanproxy-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Lanproxy related nuclei templates on
    a given target.
  name: Lanproxy Security Checks
workflows:
- matchers:
  - name: lanproxy
    subtemplates:
    - tags: lanproxy
  template: technologies/fingerprinthub-web-fingerprints.yaml
