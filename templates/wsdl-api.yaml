id: wsdl-api
info:
  author: jarijaas
  description: Detects web services that have WSDL (https://www.w3.org/TR/wsdl/)
  name: wsdl-detect
  severity: info
  tags: exposure,api
requests:
- matchers:
  - type: word
    words:
    - wsdl:definitions
  method: GET
  path:
  - '{{BaseURL}}/?wsdl'
