http:
- extractors:
  - group: 1
    name: version_by_generator
    regex:
    - (?m)https:\/\/wordpress.org\/\?v=([0-9.]+)
    type: regex
  - group: 1
    name: version_by_js
    regex:
    - wp-emoji-release\.min\.js\?ver=((\d+\.?)+)\b
    type: regex
  - group: 1
    name: version_by_css
    regex:
    - install\.min\.css\?ver=((\d+\.?)+)\b
    type: regex
  matchers:
  - dsl:
    - compare_versions(version_by_generator, '>= 6.0', '<= 6.0.1')
    - compare_versions(version_by_js, '>= 6.0', '<= 6.0.1')
    - compare_versions(version_by_css, '>= 6.0', '<= 6.0.1')
    type: dsl
  - status:
    - 200
    type: status
  matchers-condition: and
  max-redirects: 2
  method: GET
  path:
  - '{{BaseURL}}'
  - '{{BaseURL}}/wp-admin/install.php'
  - '{{BaseURL}}/feed/'
  - '{{BaseURL}}/?feed=rss2'
  redirects: true
  stop-at-first-match: true
id: wordpress-300c6ea4-4eed-4be5-abfd-ec4ad9b741d3
info:
  author: topscoder
  classification:
    cve-id: null
    cvss-metrics: CVSS:3.1/AV:N/AC:H/PR:H/UI:N/S:C/C:L/I:L/A:N
    cvss-score: 4.4
  description: 'WordPress Core, in versions up to 6.0.2, is vulnerable to Stored Cross-Site
    Scripting that can be exploited when malicious content is injected into plugin
    code that triggers when an error occurs during plugin de-activation or during
    deletion. This requires an attacker have access to the modify the error message
    that is displayed either in the plugin''s code or via a request parameter, in
    most cases it is likely to be the latter.

    '
  metadata:
    shodan-query: 'vuln:'
  name: 'WordPress Core < 6.0.2 - Stored Cross-Site Scripting via Plugin Deactivation
    and Deletion Errors

    '
  reference:
  - https://github.com/topscoder/nuclei-wordfence-cve
  - https://www.wordfence.com/threat-intel/vulnerabilities/id/300c6ea4-4eed-4be5-abfd-ec4ad9b741d3?source=api-prod
  severity: medium
  tags: cve,wordpress,wp-core,medium,production
