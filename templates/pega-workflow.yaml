id: pega-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Pega related nuclei templates on a
    given target.
  name: Pega Security Checks
workflows:
- subtemplates:
  - tags: pega
  template: http/technologies/pega-detect.yaml
