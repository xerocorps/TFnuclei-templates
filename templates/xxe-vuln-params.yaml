file:
- extensions:
  - all
  extractors:
  - regex:
    - (?i)(\?|\&)([a-z0-9-_]+)=<
    - (?i)(\?|\&)(([a-z0-9]+)((-|_)+))?(xml)=<
    type: regex
id: xxe-vuln-params
info:
  author: ayadim
  name: detect possible xml parameters
  severity: info
  tags: file,bbh,bbh-xxe,bbh-check1
