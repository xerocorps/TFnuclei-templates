id: sarg-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all SARG related nuclei templates on a
    given target.
  name: SARG Security Checks
workflows:
- matchers:
  - name: sarg
    subtemplates:
    - tags: sarg
  template: technologies/fingerprinthub-web-fingerprints.yaml
