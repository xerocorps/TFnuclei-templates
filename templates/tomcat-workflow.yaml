id: tomcat-workflow
info:
  author: pathtaga
  description: A simple workflow that runs all Tomcat related nuclei templates on
    a given target.
  name: Tomcat Security Checks
workflows:
- subtemplates:
  - null
  template: technologies/apache/tomcat-detect.yaml
