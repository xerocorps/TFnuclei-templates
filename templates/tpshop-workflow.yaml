id: tpshop-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all TPshop related nuclei templates on
    a given target.
  name: TPshop Security Checks
workflows:
- matchers:
  - name: tpshop
    subtemplates:
    - tags: tpshop
  template: technologies/fingerprinthub-web-fingerprints.yaml
