id: mida-eframework-workflow
info:
  author: CasperGN
  description: A simple workflow that runs all Mida eFramework related nuclei templates
    on a given target.
  name: Mida eFramework Security Checks
workflows:
- matchers:
  - name: mida-eframework
    subtemplates:
    - template: http/vulnerabilities/other/mida-eframework-xss.yaml
  template: http/technologies/tech-detect.yaml
