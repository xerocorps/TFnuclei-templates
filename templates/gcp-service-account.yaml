file:
- extensions:
  - all
  extractors:
  - regex:
    - '"type": "service_account"'
    type: regex
id: gcp-service-account
info:
  author: gaurang
  name: Google (GCP) Service-account
  severity: low
  tags: token,file
