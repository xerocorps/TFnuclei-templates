id: activemq-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all ActiveMQ related nuclei templates on
    a given target.
  name: ActiveMQ Security Checks
workflows:
- subtemplates:
  - tags: activemq
  template: http/exposed-panels/activemq-panel.yaml
