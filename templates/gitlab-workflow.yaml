id: gitlab-workflow
info:
  author: pdteam
  description: A simple workflow that runs all GitLab related nuclei templates on
    a given target.
  name: GitLab Security Checks
  tags: workflow
workflows:
- subtemplates:
  - tags: gitlab
  template: exposed-panels/gitlab-detect.yaml
