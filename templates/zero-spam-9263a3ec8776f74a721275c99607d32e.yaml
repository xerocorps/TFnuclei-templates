http:
- extractors:
  - group: 1
    internal: true
    name: version
    part: body
    regex:
    - '(?mi)Stable tag: ([0-9.]+)'
    type: regex
  - group: 1
    name: version
    part: body
    regex:
    - '(?mi)Stable tag: ([0-9.]+)'
    type: regex
  matchers:
  - status:
    - 200
    type: status
  - part: body
    type: word
    words:
    - zero-spam
  - dsl:
    - compare_versions(version, '< 5.4.5')
    type: dsl
  matchers-condition: and
  max-redirects: 3
  method: GET
  path:
  - '{{BaseURL}}/wp-content/plugins/zero-spam/readme.txt'
  redirects: true
id: zero-spam-9263a3ec8776f74a721275c99607d32e
info:
  author: topscoder
  classification:
    cve-id: null
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:H/UI:N/S:U/C:H/I:H/A:H
    cvss-score: 7.2
  description: The Zero Spam for WordPress plugin is vulnerable to generic SQL Injection
    via the 'type', 'country', and 's' parameters in versions up to, and including,
    5.4.4 due to insufficient escaping on the user supplied parameter and lack of
    sufficient preparation on the existing SQL query.  This makes it possible for
    authenticated attackers with administrator-level permissions to append additional
    SQL queries into already existing queries that can be used to extract sensitive
    information from the database. prepare_items
  metadata:
    fofa-query: wp-content/plugins/zero-spam/
    google-query: inurl:"/wp-content/plugins/zero-spam/"
    shodan-query: 'vuln:'
  name: Zero Spam for WordPress <= 5.4.4 - Authenticated(Administrator+) SQL Injection
  reference:
  - https://plugins.svn.wordpress.org/zero-spam/tags/5.4.5/readme.txt
  - https://plugins.svn.wordpress.org/zero-spam/tags/5.4.5/readme.md
  - https://www.highfivery.com/projects/zero-spam/
  - https://plugins.trac.wordpress.org/changeset/2909570/
  severity: high
  tags: cve,wordpress,wp-plugin,zero-spam,high
