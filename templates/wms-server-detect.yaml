id: wms-server-detect
info:
  author: pikpikcu
  metadata:
    fofa-query: app="WmsServer"
    verified: true
  name: Wms-Server detect
  severity: info
  tags: tech,wms,httpserver
requests:
- matchers:
  - part: header
    type: word
    words:
    - WMSServer/
  method: GET
  path:
  - '{{BaseURL}}'
