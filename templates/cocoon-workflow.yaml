id: cocoon-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Apache Cocoon related nuclei templates
    on a given target.
  name: Apache Cocoon Security Checks
workflows:
- subtemplates:
  - tags: cocoon
  template: technologies/apache/apache-cocoon-detect.yaml
