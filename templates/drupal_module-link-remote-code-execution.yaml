http:
- extractors:
  - group: 1
    name: version
    part: body
    regex:
    - version = "([0-9]+\.x-[0-9]+\.[0-9]+)"
    type: regex
  - dsl:
    - compare_versions(version, '9.x-99.99')
    type: dsl
  matchers:
  - condition: and
    part: body
    regex:
    - version = "([0-9]+\.x-[0-9]+\.[0-9]+)"
    type: regex
  - condition: and
    status:
    - 200
    type: status
  - condition: and
    part: body
    type: word
    words:
    - link
  matchers-condition: and
  max-redirects: 3
  method: GET
  path:
  - '{{BaseURL}}/sites/all/modules/link/link.info'
  redirects: true
id: drupal_module-link-remote-code-execution
info:
  author: me
  description: This resolves issues described in SA-CORE-2019-003 for this module.
    Not all configurations are affected. See SA-CORE-2019-003 for details.
  metadata:
    created_at: '2025-01-09'
    fofa-query: /sites/all/modules/link/
    google-query: inurl:'/sites/all/modules/link/
    impact: medium
    security-risk: "Critical 18\u2009\u2215\u200925 AC:None/A:User/CI:All/II:All/E:Theoretical/TD:Uncommon"
    type: indicator
    vulnerability: remote-code-execution
  name: drupal_module-link-remote-code-execution
  reference:
  - https://www.drupal.org/sa-contrib-2019-020
  severity: medium
  tags: drupal
