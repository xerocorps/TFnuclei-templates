http:
- extractors:
  - group: 1
    internal: true
    name: version
    part: body
    regex:
    - '(?mi)Stable tag: ([0-9.]+)'
    type: regex
  - group: 1
    name: version
    part: body
    regex:
    - '(?mi)Stable tag: ([0-9.]+)'
    type: regex
  matchers:
  - status:
    - 200
    type: status
  - part: body
    type: word
    words:
    - inboundio-marketing
  - dsl:
    - compare_versions(version, '< 2.0.1')
    type: dsl
  matchers-condition: and
  max-redirects: 3
  method: GET
  path:
  - '{{BaseURL}}/wp-content/plugins/inboundio-marketing/readme.txt'
  redirects: true
id: inboundio-marketing-0af787723a9891a971dc5412a4c7054b
info:
  author: topscoder
  classification:
    cve-id: null
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H
    cvss-score: 9.8
  description: The InBoundio Marketing plugin for WordPress is vulnerable to arbitrary
    file uploads due to missing file type validation via the csv_uploader.php file
    in versions before 2.0.1. This makes it possible for unauthenticated attackers
    to upload arbitrary files on the affected sites server which may make remote code
    execution possible.
  metadata:
    fofa-query: wp-content/plugins/inboundio-marketing/
    google-query: inurl:"/wp-content/plugins/inboundio-marketing/"
    shodan-query: 'vuln:'
  name: InBoundio Marketing < 2.0.1 - Arbitrary File Upload
  reference:
  - https://packetstormsecurity.com/files/130957/
  - https://wordpress.org/plugins/inboundio-marketing/#developers
  - https://plugins.trac.wordpress.org/changeset/1121332
  severity: critical
  tags: cve,wordpress,wp-plugin,inboundio-marketing,critical
