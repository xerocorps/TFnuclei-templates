http:
- extractors:
  - group: 1
    name: version_by_generator
    regex:
    - (?m)https:\/\/wordpress.org\/\?v=([0-9.]+)
    type: regex
  - group: 1
    name: version_by_js
    regex:
    - wp-emoji-release\.min\.js\?ver=((\d+\.?)+)\b
    type: regex
  - group: 1
    name: version_by_css
    regex:
    - install\.min\.css\?ver=((\d+\.?)+)\b
    type: regex
  matchers:
  - dsl:
    - compare_versions(version_by_generator, '<= 4.1')
    - compare_versions(version_by_js, '<= 4.1')
    - compare_versions(version_by_css, '<= 4.1')
    type: dsl
  - status:
    - 200
    type: status
  matchers-condition: and
  max-redirects: 2
  method: GET
  path:
  - '{{BaseURL}}'
  - '{{BaseURL}}/wp-admin/install.php'
  - '{{BaseURL}}/feed/'
  - '{{BaseURL}}/?feed=rss2'
  redirects: true
  stop-at-first-match: true
id: Yes-de429b2d6d2232a753a6dcc180b328fb
info:
  author: topscoder
  classification:
    cve-id: null
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:L/I:L/A:N
    cvss-score: 6.4
  description: WordPress Core is vulnerable to stored Cross-Site Scripting in versions
    up to, and including, 6.2, due to insufficient validation of the protocol in the
    response when processing oEmbed discovery. This makes it possible for authenticated
    attackers with contributor-level and above permissions to use a crafted oEmbed
    payload at a remote URL to inject arbitrary web scripts in pages that will execute
    whenever a user accesses an injected page.
  metadata:
    shodan-query: 'vuln:'
  name: WordPress Core < 6.2.1 - Authenticated (Contributor+) Stored Cross-Site Scripting
    via Embed Discovery
  reference:
  - https://core.trac.wordpress.org/changeset?sfp_email=&sfph_mail=&reponame=&old=55768%40%2F&new=55768%40%2F&sfp_email=&sfph_mail=
  - https://wordpress.org/news/2023/05/wordpress-6-2-1-maintenance-security-release/
  severity: medium
  tags: cve,wordpress,wp-core,medium
