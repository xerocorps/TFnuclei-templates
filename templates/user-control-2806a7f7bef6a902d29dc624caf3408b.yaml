http:
- extractors:
  - group: 1
    internal: true
    name: version
    part: body
    regex:
    - '(?mi)Stable tag: ([0-9.]+)'
    type: regex
  - group: 1
    name: version
    part: body
    regex:
    - '(?mi)Stable tag: ([0-9.]+)'
    type: regex
  matchers:
  - status:
    - 200
    type: status
  - part: body
    type: word
    words:
    - user-control
  - dsl:
    - compare_versions(version, '<= 2.1.0')
    type: dsl
  matchers-condition: and
  max-redirects: 3
  method: GET
  path:
  - '{{BaseURL}}/wp-content/plugins/user-control/readme.txt'
  redirects: true
id: user-control-2806a7f7bef6a902d29dc624caf3408b
info:
  author: topscoder
  classification:
    cve-id: null
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H
    cvss-score: 9.8
  description: The User Control plugin for WordPress is vulnerable to generic SQL
    Injection via the 'users' parameter in versions up to, and including, 2.1.0 due
    to insufficient escaping on the user supplied parameter and lack of sufficient
    preparation on the existing SQL query.  This makes it possible for unauthenticated
    attackers to append additional SQL queries into already existing queries that
    can be used to extract sensitive information from the database.
  metadata:
    fofa-query: wp-content/plugins/user-control/
    google-query: inurl:"/wp-content/plugins/user-control/"
    shodan-query: 'vuln:'
  name: User Control <= 2.1.0 - SQL Injection
  reference:
  - https://gist.github.com/JustThomas/cc6251400b2f7f4f7d4ed900798e9364
  - https://wpscan.com/vulnerability/7257f4f7-ab6a-433b-bd22-cf106ca4a6fb
  severity: critical
  tags: cve,wordpress,wp-plugin,user-control,critical
