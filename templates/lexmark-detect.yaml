http:
- extractors:
  - group: 1
    part: body
    regex:
    - <title>Lexmark (.*)</title>
    type: regex
  matchers:
  - part: body
    regex:
    - <title>Lexmark (.*)</title>
    type: regex
  - status:
    - 200
    type: status
  matchers-condition: and
  method: GET
  path:
  - '{{BaseURL}}'
id: lexmark-detect
info:
  author: princechaddha
  metadata:
    max-request: 1
  name: Lexmark Device Detect
  severity: info
  tags: tech,airtame,printer
