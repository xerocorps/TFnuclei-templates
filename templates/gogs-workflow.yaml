id: gogs-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all Gogs related nuclei templates on a
    given target.
  name: Gogs (Go Git Service) - Security Checks
workflows:
- subtemplates:
  - tags: gogs
  template: http/exposed-panels/gogs-panel.yaml
