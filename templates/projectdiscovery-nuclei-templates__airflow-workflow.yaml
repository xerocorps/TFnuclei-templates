id: airflow-workflow
info:
  author: pdteam
  description: A simple workflow that runs all Apache Airflow related nuclei templates
    on a given target.
  name: Apache Airflow Security Checks
workflows:
- subtemplates:
  - tags: airflow
  template: http/technologies/apache/airflow-detect.yaml
