id: zeroshell-workflow
info:
  author: daffainfo
  description: A simple workflow that runs all ZeroShell related nuclei templates
    on a given target.
  name: ZeroShell Security Checks
workflows:
- subtemplates:
  - tags: zeroshell
  template: exposed-panels/zeroshell-login.yaml
